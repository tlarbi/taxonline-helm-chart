apiVersion: v1
kind: Service
metadata:
  name: {{ include "tol-agent.fullname" . }}
  labels:
    {{- include "tol-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8001
      protocol: TCP
      name: http
  selector:
    {{- include "tol-agent.selectorLabels" . | nindent 4 }}

