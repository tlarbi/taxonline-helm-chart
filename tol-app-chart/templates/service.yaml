apiVersion: v1
kind: Service
metadata:
  name: tol-app-{{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      name: http
  selector:
    app: tol-app
