apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tol-ingestion.fullname" . }}-config
  labels:
    {{- include "tol-ingestion.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.config }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}

